//! Authoritative, deterministic village simulation.
//!
//! The initial implementation is deliberately empty. PASM defines the first
//! executable contract before simulation behaviour is introduced.

/// Identifies the headless simulation crate while the initial model is being
/// established.
pub const CRATE_NAME: &str = "village_sim";
